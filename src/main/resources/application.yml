spring:
  application:
    name: reminder

  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:postgresql://localhost:5432/reminder
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog-master.yaml

#springdoc:
#  api-docs:
#    path: /api-docs.html
#    enabled: false

telegram:
  bot:
    token: '7230385731:AAFPsBQ6mMNpQO9a7vH6iSOyjwkPYW901f4'
  api:
    url: 'https://api.telegram.org/bot'
    command: '/sendMessage'

scheduling:
  interval: 60000